# دليل مشاركة النظام مع موظفي أمانة عمّان الكبرى 🇯🇴
### طرق تشغيل ونشر "نظام كشف وتعديل مخالفات الكاميرات الرقابية"

اختر الطريقة الأنسب لك لمشاركة النظام مع الزميل أو المسؤول في أمانة عمّان الكبرى:

---

## 🚀 الطريقة 1: مشاركة فورية عبر رابط إنترنت عام (الأسرع والأسهل على الإطلاق)

إذا كنت ترغب في أن يفتح زميلك في أمانة عمّان النظام **مباشرة من هاتفه أو كمبيوتره دون أن يثبت أي شيء**:

1. تأكد أن خادم النظام يعمل على جهازك.
2. انقر نقراً مزدوجاً على الملف:
   ```
   مشاركة_عبر_الانترنت.bat
   ```
3. سيقوم البرنامج بإنشاء رابط عام مشفر ومجاني (مثال: `https://xxxx.loca.lt`).
4. **انسخ هذا الرابط وأرسله له على واتساب أو البريد الإلكتروني!**
5. سيفتح معه النظام فوراً كأي موقع إنترنت حكومي حقيقي.

---

## 🏢 الطريقة 2: مشاركة النظام على شبكة أمانة عمّان الداخلية (LAN / Wi-Fi)

إذا كنت تعمل معه في نفس المكتب أو داخل مبنى أمانة عمّان وكلاكما متصلان بنفس شبكة الإنترنت (Wi-Fi أو كيبل):

1. انقر نقراً مزدوجاً على الملف:
   ```
   مشاركة_الشبكة_المحلية.bat
   ```
2. ستظهر لك شاشة توضح عنوان جهازك على شبكة الأمانة (مثال: `http://192.168.1.45:5000`).
3. يستطيع أي موظف أو جهاز في المكتب فتح هذا الرابط في متصفحه والدخول إلى النظام مباشرة وبشكل متزامن!

---

## 💻 الطريقة 3: إرسال النظام له كملف مضغوط والتشغيل بنقرة واحدة

إذا أردت تسليمه النظام ليعمل لديه بشكل دائم ومستقل على جهازه:

1. اضغط مجلد المشروع بالكامل في ملف مضغوط `AML.zip` (باستثناء مجلد `node_modules` لتخفيف الحجم إذا لزم الأمر).
2. عند فتح المجلد على جهازه:
   - يكفيه فقط النقر مرتين على الملف:
     ```
     تشغيل_النظام.bat
     ```
   - سيقوم الملف بتشغيل الخادم وفتح المتصفح تلقائياً على واجهة أمانة عمّان.

---

## ☁️ الطريقة 4: النشر السحابي الدائم 24/7 (Cloud Hosting المجاني)

إذا أردت رفع النظام على خادم سحابي دائم يعمل على مدار الساعة برابط ثابت:

1. رفع المشروع على حساب GitHub.
2. فتح موقع [Render.com](https://render.com) أو [Railway.app](https://railway.app).
3. اختيار "New Web Service" وربط المستودع.
4. تحديد أمر التشغيل: `npm run build && npm start`.
5. سيعطيك الموقع رابطاً رسمياً ثابتاً يعمل 24 ساعة يومياً.

---

## 🔑 بيانات الدخول المعتمدة للنظام

- **حساب المدير العام (صلاحيات كاملة + إدارة الموظفين وسجل الرقابة)**:
  - اسم المستخدم: `admin`
  - كلمة المرور: `admin123`
- **حساب موظف الرقابة الآلية (إدخال، ترحيل، مقارنة اللوحات، طباعة)**:
  - اسم المستخدم: `user`
  - كلمة المرور: `user123`
