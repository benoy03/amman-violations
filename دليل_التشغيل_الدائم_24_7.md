# 🚀 دليل تشغيل نظام أمانة عمّان الكبرى على مدار الساعة (24/7 Cloud Hosting)
### مديرية الرقابة الآلية والتحكم — قسم المخالفات

لجعل النظام متاحاً **24 ساعة / 7 أيام في الأسبوع** على الإنترنت بدون الحاجة لأن يكون جهاز الكمبيوتر الخاص بك قيد التشغيل، قمنا بتجهيز كامل إعدادات النشر السحابي التلقائي عبر **Render.com** (أو **Railway.app**) مع دعم القرص الدائم لحفظ قاعدة البيانات والصور المرفوعة للأبد.

---

## 📋 الخطوات البسيطة (تستغرق 3 دقائق فقط)

### الخطوة 1: رفع المشروع إلى حسابك في GitHub
1. افتح موقع [GitHub.com](https://github.com) وسجّل الدخول.
2. أنشئ مستودعاً جديداً (New Repository) باسم مثلاً: `amman-violations`.
3. افتح موجه الأوامر (Terminal) في مجلد المشروع ونفّذ الأوامر الثلاثة التالية (استبدل الرابط برابط مستودعك):
   ```bash
   git remote add origin https://github.com/YOUR_USERNAME/amman-violations.git
   git branch -M main
   git push -u origin main
   ```

---

### الخطوة 2: الربط بـ Render (استضافة سحابية 24/7 مجاناً)
1. ادخل إلى موقع [Render.com](https://render.com) وسجل الدخول مجاناً عبر حساب GitHub.
2. اضغط على الزر الأزرق: **`New +`** ثم اختر **`Web Service`**.
3. اختر مستودع المشروع `amman-violations` الذي رفعته للتو.
4. سيتعرف Render تلقائياً على ملف `Dockerfile` و `render.yaml` المجهزين في المشروع.
5. في الإعدادات:
   - **Name**: اكتب اسماً يعجبك (مثلاً: `amman-camera-violations`).
   - **Region**: اختر `Frankfurt (EU Central)` (الأقرب جغرافياً للمملكة الأردنية وسرعة استجابة فائقة).
   - **Instance Type**: اختر `Free`.
6. اضغط في الأسفل على **`Deploy Web Service`**!

---

### الخطوة 3: الحصول على الرابط الدائم 24/7 🌍
- خلال 2 إلى 3 دقائق، سينتهي Render من البناء وسيعطيك رابطاً عاماً دائماً مشفراً (HTTPS) مثل:
  ```text
  https://amman-camera-violations.onrender.com
  ```
- هذا الرابط يعمل **24/7 من أي مكان في العالم** ومن أي جهاز (هاتف، لابتوب، أجهزة مكاتب أمانة عمّان) بدون الحاجة لتشغيل جهازك نهائياً!
- بيانات الدخول المعتمدة للنظام:
  - **المدير**: `admin` / كلمة المرور: `admin123`
  - **الموظف**: `user` / كلمة المرور: `user123`

---

## 💾 حماية وحفظ قاعدة البيانات الحقيقية
تم ضبط إعدادات Docker في المشروع بحيث يتم توجيه مسار قاعدة البيانات وملفات الصور إلى مجلد دائم:
- `DB_PATH=/data/database.sqlite`
- `UPLOADS_DIR=/data/uploads`

عند إضافة قرص تخزين مجاني (Persistent Disk) بحجم 1GB من لوحة تحكم Render على المسار `/data`، ستبقى جميع مخالفاتكم وأسماء الموظفين والصور محفوظة بشكل دائم حتى لو تمت إعادة تشغيل الخادم.
